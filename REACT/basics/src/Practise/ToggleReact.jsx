import React, { useState } from 'react'
// Exercise 2: Toggle Theme
// Build a light/dark mode toggle button that:

// Changes the background color of the page.
// Updates the button text dynamically ("Switch to Dark Mode" / "Switch to Light Mode").
const ToggleReact=()=>{
    const[isToggle,setIsToggle]=useState(false)
    return(
        <div>
            <button style={{}}></button>
        </div>
    )
}
export default ToggleReact